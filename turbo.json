{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**", "public/dist/**"],
      "dependsOn": ["^build"],
      "dotEnv": [".env"]
    },
    "typecheck": {},
    "dev": {}
  }
}
